This is the portfolio code.

app.tsx has all the relevant code that needs to be changed. Just change the contents of that.

To run and check how the website looks like : 

npm run dev

To push updates : 

git add .
git commit -m "commit message"
git push



This will build everything and auto update and publish the new website.